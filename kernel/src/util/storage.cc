#include <util/storage.hh>

// template <typename T>
// void List<T>::init_pmm() {
// 	links = pmm::alloc();
// 	memset(links, 0, pmm::pagesize);
// 	capacity = pmm::pagesize / sizeof(Link);
// 	bm.init((u64*)pmm::alloc(), capacity);
// }

// template <typename T>
// void List<T>::
